package ExercJG.Java8_Livro.cap8;

public class CarregaFrame {

    public static void main(String[] args) {

        /*|
        JFrame frame = ne|w JFrame("Uso de Botões");
        frame.setDefaultCloseOpera|tion(JFrame.EXIT_ON_CLOSE);
        frame.getContentPane().add(new GuiBotao());
        frame.setBounds(0, 0, 500, 300); 
        frame.setVisible(true);|
         */

        GuiMenuPrincipal test = new GuiMenuPrincipal();
        test.abrir();

        /*
        GuiListaFotos test = new GuiListaFotos();&
        test.GuiListaFotos();
         */
    }
}
